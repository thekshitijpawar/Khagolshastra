# Frontend Public Environment Configuration
# Only browser-safe variables prefixed with NEXT_PUBLIC_ belong here.
# NEVER put database passwords, service role keys, or private API keys here.
NEXT_PUBLIC_API_URL=http://localhost:8000
